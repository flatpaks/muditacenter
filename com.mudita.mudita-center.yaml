app-id: com.mudita.mudita-center
runtime: org.freedesktop.Platform
runtime-version: '25.08'
sdk: org.freedesktop.Sdk
base: org.electronjs.Electron2.BaseApp
base-version: '25.08'
command: run.sh
separate-locales: false
rename-desktop-file: mudita-center.desktop
rename-icon: Mudita Center
finish-args:
  - --share=network
  - --device=all
  - --socket=fallback-x11
  - --socket=wayland
  - --filesystem=xdg-download
modules:
  - name: muditacenter
    buildsystem: simple
    sources:
      - type: file
        only-arches:
          - x86_64
        url: https://github.com/mudita/mudita-center/releases/download/4.0.0/Mudita-Center.AppImage
        sha256: ecf0c1f4dba0ac470e8330407eb30bfb4bb897bf3fa3a7e6098214fd0be3d3cf
      - type: file
        path: run.sh
    build-commands:
      - chmod +x Mudita-Center.AppImage
      - ./Mudita-Center.AppImage --appimage-extract
      - desktop-file-edit --set-key=Exec --set-value="run.sh %U" squashfs-root/mudita-center.desktop
      - rm -r squashfs-root/usr/share/icons/hicolor/1024x1024
      - mv squashfs-root/* ${FLATPAK_DEST}/
      - mkdir -p "${FLATPAK_DEST}/share/applications"
      - install -m644 "${FLATPAK_DEST}/mudita-center.desktop" "${FLATPAK_DEST}/share/applications/cudita-center.desktop"
      - mv "${FLATPAK_DEST}/usr/share/icons" "${FLATPAK_DEST}/share/"
      - install -Dm0755 run.sh "${FLATPAK_DEST}/bin/run.sh"

